export enum Colors {
	primary = 'primary',
	secondary = 'secondary',
	success = 'success',
	info = 'info',
	warning = 'warning',
	danger = 'danger',
	light = 'light',
	dark = 'dark',
	body = 'body',
	muted = 'muted',
	white = 'white',
	default = body
}

export enum Semaphore {
	A = Colors.success,
	I = Colors.danger
}
